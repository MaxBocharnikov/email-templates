doctype html
html(lang="en" style="height: 100vh; font-family: 'Poppins', Arial, sans-serif; font-size: 16px; line-height: 1.5;")
    head
        title Email with Table Layout
    body(style="margin: 0; padding: 0; height: 100%; width: 100%; box-sizing: border-box;")
        table(
            style="height: 100%; max-width: 600px; margin: 0 auto; border-collapse: collapse;"
            border="0"
            cellpadding="0"
            cellspacing="0"
        )
            block content